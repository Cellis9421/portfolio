# name: Build and Deploy to GitHub Pages

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v2
#       with:
#         persist-credentials: false

#     - name: Setup Node.js
#       uses: actions/setup-node@v2
#       with:
#         node-version: '20.11.1'  # Set to your Node.js version

#     - name: Install Dependencies
#       run: npm install

#     - name: Build and Export
#       run: npm run build-compress

#     - name: Push to GitHub
#       env:
#           PERSONAL_TOKEN: ${{ secrets.GH_P_TOKEN }}
#       run: |
#         git config --global user.name 'GitHub Actions'
#         git config --global user.email 'actions@github.com'
#         git add docs/
#         git commit -m 'Deploy Next.js app to GitHub Pages [skip ci]' --allow-empty
#         git push https://${PERSONAL_TOKEN}@github.com/${{ github.repository }}.git main

#     - name: Deploy to GitHub Pages
#       uses: JamesIves/github-pages-deploy-action@3.7.1
#       with:
#         ACCESS_TOKEN: ${{ secrets.GH_P_TOKEN }}
#         BRANCH: gh-pages
#         FOLDER: docs
#         CLEAN: true
